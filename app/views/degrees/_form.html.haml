= semantic_form_for @degree do |f|
  -if @degree.errors.any?
    #error_explanation
      %h2= "#{pluralize(@degree.errors.count, "error")} prohibited this degree from being saved:"
      %ul
        - @degree.errors.full_messages.each do |msg|
          %li= msg

  - f.inputs do
    = f.input :name
    = f.input :initials
    
  - f.buttons do
    = f.commit_button
